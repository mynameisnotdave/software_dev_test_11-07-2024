const { Book, Library } = require('../library');

describe('Book', () => {
  test('constructor sets properties correctly', () => {
    // Implement test
  });
});

describe('Library', () => {
  test('addBook increases total books', () => {
    // Implement test
  });

  test('findBookByISBN returns correct book', () => {
    // Implement test
  });

  test('findBooksByAuthor returns correct books', () => {
    // Implement test
  });
});